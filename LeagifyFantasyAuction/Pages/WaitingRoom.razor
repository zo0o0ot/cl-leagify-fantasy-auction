@page "/auction/{auctionId:int}/waiting-room"
@using System.Net.Http.Json
@using Microsoft.AspNetCore.SignalR.Client
@inject HttpClient Http
@inject IJSRuntime JSRuntime
@inject NavigationManager Navigation
@implements IAsyncDisposable

<PageTitle>Waiting Room - Leagify Fantasy Auction</PageTitle>

<div class="waiting-room-container">
    @if (isLoading)
    {
        <div class="loading-container">
            <FluentProgressRing />
            <FluentLabel Typo="Typography.Body" Style="margin-top: 16px;">
                Loading waiting room...
            </FluentLabel>
        </div>
    }
    else if (!string.IsNullOrEmpty(errorMessage))
    {
        <FluentMessageBar Intent="MessageIntent.Error">
            @errorMessage
        </FluentMessageBar>
    }
    else if (auctionData != null && userData != null)
    {
        <!-- Waiting Room Header -->
        <div class="waiting-room-header">
            <div style="display: flex; justify-content: space-between; align-items: center;">
                <div>
                    <FluentLabel Typo="Typography.PageTitle">
                        🏈 @auctionData.AuctionName - Waiting Room
                    </FluentLabel>
                    <FluentLabel Style="color: #666; margin-top: 8px;">
                        Practice bidding and prepare for the draft
                    </FluentLabel>
                </div>
                <div style="display: flex; align-items: center; gap: 16px;">
                    <FluentBadge Appearance="Appearance.Accent"
                                 BackgroundColor="#0078d4"
                                 Color="white"
                                 Style="font-size: 16px; padding: 8px 16px;">
                        👤 @userData.DisplayName
                    </FluentBadge>
                    <FluentBadge Appearance="@(isConnectedToSignalR ? Appearance.Accent : Appearance.Neutral)"
                                 BackgroundColor="@(isConnectedToSignalR ? "#28a745" : "#6c757d")"
                                 Color="white">
                        @(isConnectedToSignalR ? "🟢 Live" : "⚫ Offline")
                    </FluentBadge>
                </div>
            </div>
        </div>

        <!-- Test Bidding Section -->
        <FluentCard Style="margin-bottom: 24px;">
            <div class="test-bidding-section">
                <FluentLabel Typo="Typography.Header">
                    🎯 Test Bidding - @(testSchool?.Name ?? "Vermont A&M")
                </FluentLabel>
                <FluentLabel Style="color: #666; margin-top: 8px; margin-bottom: 16px;">
                    Practice bidding to test your connection. Multiple test schools available (Vermont A&M, Luther College, Oxford University, UNI, DeVry). These bids don't affect your budget.
                </FluentLabel>

                @if (testSchool != null)
                {
                    <div class="current-bid-display">
                        <FluentLabel Typo="Typography.Body" Style="font-weight: 600;">
                            Current Test Bid: $@testSchool.CurrentBid.ToString("N0")
                            @if (!string.IsNullOrEmpty(testSchool.CurrentBidderName))
                            {
                                <span style="color: #0078d4;">(@testSchool.CurrentBidderName)</span>
                            }
                        </FluentLabel>
                        <FluentLabel Style="color: #666; font-size: 14px; margin-top: 4px;">
                            Your Test Budget: $200 | Remaining: $@((200 - (userData.HasTestedBidding ? testSchool.CurrentBid : 0)).ToString("N0"))
                        </FluentLabel>
                    </div>

                    <div class="bid-buttons" style="margin-top: 16px;">
                        <FluentButton Appearance="Appearance.Accent"
                                      OnClick="@(() => PlaceTestBid(testSchool.CurrentBid + 1))"
                                      Disabled="@isPlacingBid">
                            Bid $@((testSchool.CurrentBid + 1).ToString("N0"))
                        </FluentButton>
                        <FluentButton Appearance="Appearance.Accent"
                                      OnClick="@(() => PlaceTestBid(testSchool.CurrentBid + 5))"
                                      Disabled="@isPlacingBid">
                            Bid $@((testSchool.CurrentBid + 5).ToString("N0"))
                        </FluentButton>
                        <FluentButton Appearance="Appearance.Accent"
                                      OnClick="@(() => PlaceTestBid(testSchool.CurrentBid + 10))"
                                      Disabled="@isPlacingBid">
                            Bid $@((testSchool.CurrentBid + 10).ToString("N0"))
                        </FluentButton>
                        <FluentButton Appearance="Appearance.Neutral"
                                      OnClick="@PassOnBidding"
                                      Disabled="@(isPlacingBid || isPassingOnBid)">
                            @(isPassingOnBid ? "Passing..." : "Pass")
                        </FluentButton>
                    </div>

                    @if (testBidHistory.Any())
                    {
                        <div class="bid-history" style="margin-top: 16px;">
                            <FluentLabel Typo="Typography.Body" Style="font-weight: 600; margin-bottom: 8px;">
                                Recent Test Bids:
                            </FluentLabel>
                            @foreach (var bid in testBidHistory.Take(5))
                            {
                                <div style="margin: 4px 0; color: #666; font-size: 14px;">
                                    • @bid.BidderName bid $@bid.Amount.ToString("N0")
                                </div>
                            }
                        </div>
                    }

                    @if (wonTestSchools.Any())
                    {
                        <div class="won-schools" style="margin-top: 16px; padding: 12px; background: #e8f5e9; border-radius: 6px; border-left: 3px solid #28a745;">
                            <FluentLabel Typo="Typography.Body" Style="font-weight: 600; margin-bottom: 8px; color: #28a745;">
                                🏆 Your Test School Wins:
                            </FluentLabel>
                            @foreach (var win in wonTestSchools)
                            {
                                <div style="margin: 4px 0; color: #2e7d32; font-size: 14px;">
                                    • <strong>@win.SchoolName</strong> - $@win.Amount.ToString("N0")
                                </div>
                            }
                        </div>
                    }
                }
            </div>
        </FluentCard>

        <!-- Participants & Bidding Status -->
        @if (participants.Any())
        {
            <FluentCard Style="margin-bottom: 24px;">
                <div class="participants-section">
                    <FluentLabel Typo="Typography.Header">
                        👥 Participants & Bidding Status
                    </FluentLabel>
                    <FluentLabel Style="color: #666; margin-top: 8px; margin-bottom: 16px;">
                        See who's actively bidding and who has passed on the current test bid
                    </FluentLabel>

                    <div class="participants-list">
                        @foreach (var participant in participants)
                        {
                            <div class="participant-item">
                                <div class="participant-info">
                                    <FluentLabel Style="font-weight: 600;">@participant.DisplayName</FluentLabel>
                                    @if (participant.HasPassedOnTestBid)
                                    {
                                        <FluentBadge Appearance="Appearance.Neutral"
                                                     BackgroundColor="#6c757d"
                                                     Color="white"
                                                     Style="margin-left: 8px;">
                                            Passed
                                        </FluentBadge>
                                    }
                                    else
                                    {
                                        <FluentBadge Appearance="Appearance.Accent"
                                                     BackgroundColor="#28a745"
                                                     Color="white"
                                                     Style="margin-left: 8px;">
                                            Active
                                        </FluentBadge>
                                    }
                                </div>
                                <div class="participant-status">
                                    @if (participant.IsConnected)
                                    {
                                        <span style="color: #28a745;">🟢 Online</span>
                                    }
                                    else
                                    {
                                        <span style="color: #6c757d;">⚫ Offline</span>
                                    }
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </FluentCard>
        }

        <!-- Readiness Status -->
        <FluentCard Style="margin-bottom: 24px;">
            <div class="readiness-section">
                <FluentLabel Typo="Typography.Header">
                    📋 Readiness Status
                </FluentLabel>

                <div style="margin: 16px 0;">
                    <div class="readiness-indicator">
                        <span class="readiness-icon">@(userData.HasTestedBidding ? "✅" : "❓")</span>
                        <FluentLabel Style="margin-left: 8px;">
                            Technical: @(userData.HasTestedBidding ? "Bidding Tested" : "Not Tested Yet")
                        </FluentLabel>
                    </div>
                    <div class="readiness-indicator" style="margin-top: 8px;">
                        <span class="readiness-icon">@(userData.IsReadyToDraft ? "🏈" : "❓")</span>
                        <FluentLabel Style="margin-left: 8px;">
                            Personal: @(userData.IsReadyToDraft ? "Ready to Draft" : "Still Preparing")
                        </FluentLabel>
                    </div>
                </div>

                <FluentButton Appearance="@(userData.IsReadyToDraft ? Appearance.Neutral : Appearance.Accent)"
                              OnClick="@ToggleReadiness"
                              Disabled="@isUpdatingReadiness"
                              Style="width: 100%; margin-top: 16px;">
                    @if (userData.IsReadyToDraft)
                    {
                        <span>❌ I Need More Time</span>
                    }
                    else
                    {
                        <span>🏈 I'm Ready to Draft</span>
                    }
                </FluentButton>
            </div>
        </FluentCard>

        <!-- School Preview -->
        <FluentCard Style="margin-bottom: 24px;">
            <div class="school-preview-section">
                <FluentLabel Typo="Typography.Header">
                    🏫 Available Schools (Read Only)
                </FluentLabel>
                <FluentLabel Style="color: #666; margin-top: 8px; margin-bottom: 16px;">
                    Review schools available for the draft. Test bidding available on: Vermont A&M, Luther College, Oxford University, University of Northern Iowa, and DeVry University.
                </FluentLabel>

                @if (schools.Any())
                {
                    <div class="school-grid">
                        @foreach (var school in schools.Take(12))
                        {
                            <div class="school-card">
                                <FluentLabel Style="font-weight: 600; font-size: 14px;">@school.Name</FluentLabel>
                                <FluentLabel Style="color: #666; font-size: 12px;">@school.Conference</FluentLabel>
                            </div>
                        }
                        @if (schools.Count > 12)
                        {
                            <div class="school-card" style="background: #f5f5f5;">
                                <FluentLabel Style="color: #666; font-size: 14px;">
                                    +@(schools.Count - 12) more schools
                                </FluentLabel>
                            </div>
                        }
                    </div>
                }
                else
                {
                    <FluentMessageBar Intent="MessageIntent.Info" Style="margin-top: 16px;">
                        <strong>No schools imported yet.</strong>
                        <div style="margin-top: 8px;">
                            The Auction Master needs to import schools from a CSV file before the draft can begin.
                            Once schools are imported, they will appear here.
                        </div>
                    </FluentMessageBar>
                }
            </div>
        </FluentCard>

        <!-- Nomination Order -->
        @if (nominationOrder.Any())
        {
            <FluentCard>
                <div class="nomination-order-section">
                    <FluentLabel Typo="Typography.Header">
                        📝 Draft Order
                    </FluentLabel>
                    <FluentLabel Style="color: #666; margin-top: 8px; margin-bottom: 16px;">
                        This is the order teams will nominate schools
                    </FluentLabel>

                    <div class="order-list">
                        @foreach (var (displayName, index) in nominationOrder.Select((name, idx) => (name, idx)))
                        {
                            <div class="order-item">
                                <span class="order-number">@(index + 1).</span>
                                <FluentLabel>@displayName</FluentLabel>
                            </div>
                        }
                    </div>
                </div>
            </FluentCard>
        }
    }
</div>

@code {
    [Parameter]
    public int AuctionId { get; set; }

    private bool isLoading = true;
    private string errorMessage = string.Empty;
    private bool isPlacingBid = false;
    private bool isUpdatingReadiness = false;

    private AuctionData? auctionData;
    private UserData? userData;
    private TestSchoolData? testSchool;
    private List<TestBidData> testBidHistory = new();
    private List<SchoolData> schools = new();
    private List<string> nominationOrder = new();
    private List<ParticipantData> participants = new();
    private List<WonTestSchoolData> wonTestSchools = new();

    private HubConnection? hubConnection;
    private bool isConnectedToSignalR = false;
    private bool isPassingOnBid = false;

    protected override async Task OnInitializedAsync()
    {
        await LoadWaitingRoomData();
        await InitializeSignalR();
    }

    public async ValueTask DisposeAsync()
    {
        if (hubConnection is not null)
        {
            await hubConnection.DisposeAsync();
        }
    }

    private async Task InitializeSignalR()
    {
        try
        {
            // Get session from localStorage
            var sessionJson = await JSRuntime.InvokeAsync<string?>("localStorage.getItem", "auctionSession");
            if (string.IsNullOrEmpty(sessionJson))
            {
                Console.WriteLine("No session found for SignalR connection");
                return;
            }

            var session = System.Text.Json.JsonSerializer.Deserialize<UserSession>(sessionJson);
            if (session == null || session.AuctionId != AuctionId)
            {
                Console.WriteLine("Invalid session for SignalR connection");
                return;
            }

            Console.WriteLine($"🔌 Initializing SignalR connection for auction {AuctionId}");

            // Build SignalR connection
            hubConnection = new HubConnectionBuilder()
                .WithUrl(Navigation.ToAbsoluteUri("/api/signalr"))
                .WithAutomaticReconnect()
                .Build();

            // Register event handlers BEFORE starting connection
            // Note: First parameter is always auction ID for filtering
            hubConnection.On<int, string, decimal, DateTime>("TestBidPlaced", (messageAuctionId, bidderName, amount, bidDate) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"📢 SignalR: Test bid received - {bidderName} bid ${amount}");

                // Update test school current bid
                if (testSchool != null)
                {
                    testSchool.CurrentBid = amount;
                    testSchool.CurrentBidderName = bidderName;
                }

                // Add to bid history
                testBidHistory.Insert(0, new TestBidData
                {
                    BidderName = bidderName,
                    Amount = amount,
                    BidDate = bidDate
                });

                // Keep only last 10 bids
                if (testBidHistory.Count > 10)
                {
                    testBidHistory = testBidHistory.Take(10).ToList();
                }

                // Reset all participants' pass status when a new bid is placed
                foreach (var participant in participants)
                {
                    participant.HasPassedOnTestBid = false;
                }

                // Update current user's pass status if needed
                if (userData != null)
                {
                    userData.HasPassedOnTestBid = false;
                }

                InvokeAsync(StateHasChanged);
            });

            hubConnection.On<int, string, bool>("ReadinessUpdated", (messageAuctionId, displayName, isReady) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"📢 SignalR: Readiness updated - {displayName} is {(isReady ? "ready" : "not ready")}");
                // Could update a participants list here if we had one
                InvokeAsync(StateHasChanged);
            });

            hubConnection.On<int, string>("UserJoinedWaitingRoom", (messageAuctionId, displayName) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"📢 SignalR: {displayName} joined the waiting room");
                InvokeAsync(StateHasChanged);
            });

            hubConnection.On<int, string>("UserTestedBidding", (messageAuctionId, displayName) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"📢 SignalR: {displayName} tested bidding for the first time");
                InvokeAsync(StateHasChanged);
            });

            hubConnection.On<int, string>("UserPassedOnTestBid", (messageAuctionId, displayName) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"📢 SignalR: {displayName} passed on test bid");

                // Update participant's pass status
                var participant = participants.FirstOrDefault(p => p.DisplayName == displayName);
                if (participant != null)
                {
                    participant.HasPassedOnTestBid = true;
                }

                // Update current user's status if it's them
                if (userData != null && userData.DisplayName == displayName)
                {
                    userData.HasPassedOnTestBid = true;
                }

                InvokeAsync(StateHasChanged);
            });

            hubConnection.On<int, string, string, decimal, string>("TestBiddingCompleted",
                async (messageAuctionId, schoolName, winner, winningBid, nextSchool) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"📢 SignalR: Test bidding completed on {schoolName}! Winner: {winner} (${winningBid}). Next: {nextSchool}");

                // Refresh all waiting room data to get the new school and reset states
                await LoadWaitingRoomData();

                // Note: We're not showing a toast here as the UI will auto-update
                // If desired, could add JSRuntime notification here
            });

            hubConnection.On<int>("TestBidsReset", async (messageAuctionId) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"📢 SignalR: Test bids have been reset by auction master");

                // Refresh all waiting room data to clear bids and reset states
                await LoadWaitingRoomData();

                // Note: We're not showing a toast here as the UI will auto-update
                // If desired, could add JSRuntime notification here
            });

            hubConnection.On<int, string>("AuctionStarted", (messageAuctionId, auctionName) =>
            {
                // Filter messages for this auction only
                if (messageAuctionId != AuctionId) return;

                Console.WriteLine($"🚀 SignalR: Auction '{auctionName}' has started!");

                // Navigate to the live auction page
                InvokeAsync(() =>
                {
                    Navigation.NavigateTo($"/auction/{AuctionId}/live");
                });
            });

            // Start connection
            await hubConnection.StartAsync();
            isConnectedToSignalR = true;
            Console.WriteLine("✅ SignalR connected successfully");
            Console.WriteLine($"✅ Listening for auction {AuctionId} messages");

            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"❌ SignalR connection error: {ex.Message}");
            isConnectedToSignalR = false;
            // Don't block the UI if SignalR fails - fall back to polling
        }
    }

    private async Task LoadWaitingRoomData()
    {
        try
        {
            isLoading = true;
            errorMessage = string.Empty;
            StateHasChanged();

            // Get session from localStorage
            var sessionJson = await JSRuntime.InvokeAsync<string?>("localStorage.getItem", "auctionSession");
            if (string.IsNullOrEmpty(sessionJson))
            {
                Navigation.NavigateTo("/join");
                return;
            }

            var session = System.Text.Json.JsonSerializer.Deserialize<UserSession>(sessionJson);
            if (session == null || session.AuctionId != AuctionId)
            {
                Navigation.NavigateTo("/join");
                return;
            }

            // Set auth header
            Http.DefaultRequestHeaders.Remove("X-Auction-Token");
            Http.DefaultRequestHeaders.Add("X-Auction-Token", session.SessionToken);

            // Load waiting room data (we'll implement this API endpoint next)
            var response = await Http.GetAsync($"/api/auction/{AuctionId}/waiting-room");

            if (response.IsSuccessStatusCode)
            {
                var data = await response.Content.ReadFromJsonAsync<WaitingRoomResponse>();
                if (data != null)
                {
                    auctionData = data.Auction;
                    userData = data.User;
                    testSchool = data.TestSchool;
                    testBidHistory = data.TestBidHistory ?? new List<TestBidData>();
                    schools = data.Schools ?? new List<SchoolData>();
                    nominationOrder = data.NominationOrder ?? new List<string>();
                    participants = data.Participants ?? new List<ParticipantData>();
                    wonTestSchools = data.UserWonTestSchools ?? new List<WonTestSchoolData>();
                }
            }
            else if (response.StatusCode == System.Net.HttpStatusCode.Unauthorized)
            {
                await JSRuntime.InvokeVoidAsync("localStorage.removeItem", "auctionSession");
                Navigation.NavigateTo("/join");
            }
            else
            {
                errorMessage = "Unable to load waiting room data";
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error loading waiting room: {ex.Message}";
            Console.WriteLine($"Waiting room error: {ex}");
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task PlaceTestBid(decimal amount)
    {
        try
        {
            isPlacingBid = true;
            StateHasChanged();

            var response = await Http.PostAsJsonAsync($"/api/auction/{AuctionId}/test-bid", new { Amount = amount });

            if (response.IsSuccessStatusCode)
            {
                await LoadWaitingRoomData(); // Refresh data
            }
            else
            {
                errorMessage = "Failed to place test bid";
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error placing bid: {ex.Message}";
        }
        finally
        {
            isPlacingBid = false;
            StateHasChanged();
        }
    }

    private async Task PassOnBidding()
    {
        try
        {
            isPassingOnBid = true;
            StateHasChanged();

            Console.WriteLine("User passing on test bidding...");

            var response = await Http.PostAsync($"/api/auction/{AuctionId}/test-bid/pass", null);

            if (response.IsSuccessStatusCode)
            {
                Console.WriteLine("✅ Pass recorded successfully");
                // SignalR will update the UI via UserPassedOnTestBid event
            }
            else
            {
                errorMessage = "Failed to record pass action";
                Console.WriteLine($"❌ Pass failed: {response.StatusCode}");
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error passing on bid: {ex.Message}";
            Console.WriteLine($"❌ Pass error: {ex.Message}");
        }
        finally
        {
            isPassingOnBid = false;
            StateHasChanged();
        }
    }

    private async Task ToggleReadiness()
    {
        try
        {
            isUpdatingReadiness = true;
            StateHasChanged();

            // Get session token and set auth header
            var sessionJson = await JSRuntime.InvokeAsync<string?>("localStorage.getItem", "auctionSession");
            if (!string.IsNullOrEmpty(sessionJson))
            {
                var session = System.Text.Json.JsonSerializer.Deserialize<UserSession>(sessionJson);
                if (session != null)
                {
                    Http.DefaultRequestHeaders.Remove("X-Auction-Token");
                    Http.DefaultRequestHeaders.Add("X-Auction-Token", session.SessionToken);
                }
            }

            var currentReadiness = userData!.IsReadyToDraft;
            var newReadiness = !currentReadiness;

            Console.WriteLine($"🔄 ToggleReadiness: Current={currentReadiness}, New={newReadiness}, Sending IsReady={newReadiness}");

            var response = await Http.PostAsJsonAsync($"/api/auction/{AuctionId}/ready-status", new { IsReady = newReadiness });

            Console.WriteLine($"📡 Response status: {response.StatusCode}");

            if (response.IsSuccessStatusCode)
            {
                userData.IsReadyToDraft = newReadiness;
                Console.WriteLine($"✅ Local state updated to: {userData.IsReadyToDraft}");
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                errorMessage = "Failed to update readiness status";
                Console.WriteLine($"❌ Ready status update failed: {response.StatusCode}, Error: {errorContent}");
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error updating readiness: {ex.Message}";
            Console.WriteLine($"❌ Ready status error: {ex}");
        }
        finally
        {
            isUpdatingReadiness = false;
            StateHasChanged();
        }
    }

    // Data models
    private class UserSession
    {
        public int UserId { get; set; }
        public int AuctionId { get; set; }
        public string DisplayName { get; set; } = string.Empty;
        public string SessionToken { get; set; } = string.Empty;
    }

    private class WaitingRoomResponse
    {
        public AuctionData Auction { get; set; } = new();
        public UserData User { get; set; } = new();
        public TestSchoolData TestSchool { get; set; } = new();
        public List<TestBidData> TestBidHistory { get; set; } = new();
        public List<SchoolData> Schools { get; set; } = new();
        public List<string> NominationOrder { get; set; } = new();
        public List<ParticipantData> Participants { get; set; } = new();
        public List<WonTestSchoolData> UserWonTestSchools { get; set; } = new();
    }

    private class AuctionData
    {
        public string AuctionName { get; set; } = string.Empty;
        public string Status { get; set; } = string.Empty;
    }

    private class UserData
    {
        public string DisplayName { get; set; } = string.Empty;
        public bool HasTestedBidding { get; set; }
        public bool IsReadyToDraft { get; set; }
        public bool HasPassedOnTestBid { get; set; }
    }

    private class TestSchoolData
    {
        public int SchoolId { get; set; }
        public string Name { get; set; } = "Vermont A&M";
        public decimal CurrentBid { get; set; }
        public string CurrentBidderName { get; set; } = string.Empty;
    }

    private class TestBidData
    {
        public string BidderName { get; set; } = string.Empty;
        public decimal Amount { get; set; }
        public DateTime BidDate { get; set; }
    }

    private class SchoolData
    {
        public string Name { get; set; } = string.Empty;
        public string Conference { get; set; } = string.Empty;
    }

    private class ParticipantData
    {
        public string DisplayName { get; set; } = string.Empty;
        public bool HasTestedBidding { get; set; }
        public bool IsReadyToDraft { get; set; }
        public bool HasPassedOnTestBid { get; set; }
        public bool IsConnected { get; set; }
    }

    private class WonTestSchoolData
    {
        public int SchoolId { get; set; }
        public string SchoolName { get; set; } = string.Empty;
        public decimal Amount { get; set; }
        public DateTime WonDate { get; set; }
    }
}

<style>
    .waiting-room-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 24px;
    }

    .loading-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 400px;
    }

    .waiting-room-header {
        margin-bottom: 32px;
        text-align: center;
    }

    .test-bidding-section,
    .readiness-section,
    .school-preview-section,
    .nomination-order-section {
        padding: 20px;
    }

    .current-bid-display {
        background: #f5f5f5;
        padding: 16px;
        border-radius: 8px;
        margin-bottom: 16px;
    }

    .bid-buttons {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
    }

    .bid-history {
        background: #f9f9f9;
        padding: 12px;
        border-radius: 6px;
        border-left: 3px solid #0078d4;
    }

    .readiness-indicator {
        display: flex;
        align-items: center;
        padding: 8px;
        background: #f5f5f5;
        border-radius: 6px;
    }

    .readiness-icon {
        font-size: 24px;
    }

    .school-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
        gap: 12px;
        margin-top: 16px;
    }

    .school-card {
        padding: 12px;
        border: 1px solid #e1e1e1;
        border-radius: 6px;
        background: white;
    }

    .order-list {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .order-item {
        display: flex;
        align-items: center;
        padding: 12px;
        background: #f5f5f5;
        border-radius: 6px;
    }

    .order-number {
        font-weight: 600;
        margin-right: 12px;
        color: #0078d4;
        min-width: 30px;
    }

    .participants-section {
        padding: 20px;
    }

    .participants-list {
        display: flex;
        flex-direction: column;
        gap: 12px;
        margin-top: 16px;
    }

    .participant-item {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px;
        background: #f5f5f5;
        border-radius: 6px;
    }

    .participant-info {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .participant-status {
        font-size: 14px;
    }
</style>
