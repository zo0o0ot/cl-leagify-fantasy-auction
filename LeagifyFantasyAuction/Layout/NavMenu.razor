<style>
    .navmenu { width: 250px; background: #f5f5f5; border-right: 1px solid #ddd; padding: 20px 0; }
    .navmenu-header { padding: 0 20px 16px; border-bottom: 1px solid #ddd; margin-bottom: 16px; }
    .navmenu-toggle { display: none; }
    .nav-item { display: block; padding: 12px 20px; text-decoration: none; color: #333; transition: background 0.2s; }
    .nav-item:hover { background: #e3f2fd; text-decoration: none; color: #0078d4; }
    .nav-item.active { background: #0078d4; color: white; }
    .nav-item.active:hover { background: #106ebe; }
</style>

<div class="navmenu">
    <div class="navmenu-header">
        <h5>Navigation</h5>
    </div>
    <nav class="sitenav" aria-labelledby="main-menu">
        <NavLink class="nav-item" href="/" Match="NavLinkMatch.All">
            🏠 Home
        </NavLink>
        <NavLink class="nav-item" href="/join">
            🎯 Join Auction
        </NavLink>
    </nav>
</div>
